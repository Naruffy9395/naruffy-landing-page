/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/naruffy-landing-page',
  images: { unoptimized: true },
};

export default nextConfig;
